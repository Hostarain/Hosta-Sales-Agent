server:
  port: 8087

spring:
  application:
    name: jc-sales-agent
  main:
    web-application-type: servlet   # ???? webflux ????? servlet ????? actuator ??????
  data:
    redis:
      host: 117.72.56.40
      port: 6379
      password: jichi1234   # ?? Redis ?????????
      database: 0

  # ???
  datasource:
    url: jdbc:mysql://localhost:3306/SalesAgent
    username: root
    password: 121314
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  # SQL ?????? + ?????IF NOT EXISTS + INSERT IGNORE ???????
#  sql:
#    init:
#      mode: always
#      schema-locations: classpath:db/schema.sql
#      data-locations: classpath:db/data.sql

  # JPA
  jpa:
    hibernate:
      ddl-auto: none            # ?? schema.sql ????? JPA ????
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    defer-datasource-initialization: true  # ? JPA ??????? SQL ??

# LangChain4j OpenAI ???? ? DashScope
langchain4j:
  open-ai:
    chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-482d4d1c0c8f498785e5ad96d3ae6861
      model-name: qwen-max
      temperature: 0.1          # ????????????????
      max-tokens: 2048
    streaming-chat-model:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      api-key: sk-482d4d1c0c8f498785e5ad96d3ae6861
      model-name: qwen-max
      temperature: 0.1
      max-tokens: 2048

# Sa-Token ??????????
sa-token:
  token-name: Authorization
  timeout: 86400
  is-concurrent: true
  is-share: true
  token-style: uuid
  is-log: false

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

  endpoint:
    health:
      show-details: always

# ???????
sales-agent:
  tool:
    anomaly-threshold-days: 5
    trend-drop-threshold: 0.3

logging:
  level:
    com.jichi.salesAgent: DEBUG
    dev.langchain4j: INFO
app:
  auth:
    enabled: false
